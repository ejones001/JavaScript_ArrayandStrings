// Create a string with some text content
let message = "Hello, World!";

// Obtain the length of the string using the length property
let lengthOfMessage = message.length;

console.log('Length of the string:', lengthOfMessage); // Output: 13
